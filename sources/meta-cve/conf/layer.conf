BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "cve"
BBFILE_PATTERN_cve = "^${LAYERDIR}/"
BBFILE_PRIORITY_cve = "6"

LAYERSERIES_COMPAT_cve = "scarthgap"